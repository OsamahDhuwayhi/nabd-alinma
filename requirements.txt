# نبض الإنماء — المكتبات المطلوبة (كلها مجانية / مفتوحة المصدر 100%)
# ملاحظة: لا يوجد أي مكتبة مدفوعة هنا.

# --- Phase 1: السحب ---
google-play-scraper      # سحب التقييمات من متجر جوجل بلاي (مجاني)
pandas                   # معالجة البيانات والجداول
# ملاحظة: نكتشف اللغة (عربي/إنجليزي) بفحص نوع الحروف داخل السكربت مباشرة،
#         وهو أدق من langdetect مع النصوص القصيرة، فلا حاجة لمكتبة إضافية.

# --- Phase 2: التصنيف والأولوية ---
scikit-learn             # التصنيف والتجميع (KMeans)
sentence-transformers    # نماذج تعمل محليًا مجانًا (اختياري للترقية الدلالية)

# --- Phase 3: توليد الردود (مجاني فقط) ---
groq                     # واجهة Groq المجانية (llama-3.3-70b) — تحتاج مفتاح مجاني
python-dotenv            # لتحميل GROQ_API_KEY تلقائيًا من ملف .env المحلي

# --- Phase 4: لوحة العرض ---
streamlit                # لوحة تحكم تفاعلية (مجانية التشغيل والاستضافة)
plotly                   # رسوم بيانية تفاعلية داخل اللوحة
